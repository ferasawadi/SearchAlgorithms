package com.yafatek;import java.util.logging.Logger;public class CircularArraySearch {	private static Logger logger = Logger.getLogger(CircularArraySearch.class.getName());	public static void main(String[] args) {		//TODO		// Call Search Method here.		logger.info("::::: Circular Array Search :::::");		int[] array = {9, 10, 2, 5, 6, 8};		int key = 5;		int index = circularArrayBinarySearch(array, key);		logger.info(index == -1 ? "Element Not Found!" : String.format("Element Found At index: (%s)", index));	}	private static int circularArrayBinarySearch(int[] array, int elementToSearch) {		int first = 0;		int last = array.length - 1;		while (first <= last) {			int middle = (first + last) / 2;			if (elementToSearch == array[middle]) return middle;			if (array[middle] <= array[last]) {				if (elementToSearch > array[middle] && elementToSearch <= array[last]) first = middle + 1;				else last = middle - 1;			} else {				if (elementToSearch >= array[first] && elementToSearch < array[middle]) last = middle - 1;				else first = middle + 1;			}		}		return -1;	}}