package com.yafatek;import java.util.logging.Logger;public class FirstAndLastOccurenceOfNumber {	private static Logger logger = Logger.getLogger(FirstAndLastOccurenceOfNumber.class.getName());	public static void main(String[] args) {		//TODO		// Getting the First and last occurenece of a number in an array.		int[] array = {2, 5, 5, 5, 6, 6, 8, 9, 9, 9};		int toFind = 5;		int result = findFirstAndLast(array, toFind);		int lastOccurrence = findLastOccurrence(array, toFind);		logger.info(result == -1 ? "Element Not Found!" : "First Occurrence of the Element Found At Index: (" + result + ")");		logger.info(lastOccurrence == -1 ? "Element Not Found!" : "last occurrence to the element found at index: " + lastOccurrence);	}	private static int findFirstAndLast(int[] array, int elementToSearch) {		int result = -1;		int first = 0;		int last = array.length - 1;		while (first <= last) {			int mid = (first + last) / 2;			if (elementToSearch == array[mid]) {				result = mid;				last = mid - 1;			} else if (elementToSearch < array[mid]) last = mid - 1;			else first = mid + 1;		}		return result;	}	private static int findLastOccurrence(int[] array, int elementToSearch) {		int result = -1;		int first = 0;		int last = array.length - 1;		while (first <= last) {			int mid = (first + last) / 2;			if (elementToSearch == array[mid]) {				result = mid;				first = mid + 1;			} else if (elementToSearch < array[mid]) last = mid - 1;			else first = mid + 1;		}		return result;	}}