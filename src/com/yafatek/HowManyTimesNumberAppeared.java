package com.yafatek;import java.util.logging.Logger;public class HowManyTimesNumberAppeared {	private static Logger logger = Logger.getLogger(HowManyTimesNumberAppeared.class.getName());	public static void main(String[] args) {		// TODO		//  call the Method here.		int[] array = {2, 5, 5, 5, 6, 6, 8, 9, 9, 9};		int toFind = 5;		int first = howManyOccurrences(array, toFind, true);		int last = howManyOccurrences(array, toFind, false);		int result = last - first + 1;		logger.info(first == -1 ? "Element Not Found!" : "Element: (" + toFind + ") Occurs: (" + result + ") Times.");	}	private static int howManyOccurrences(int[] array, int elementToSearch, boolean leftOrRightFlag) {		int result = -1;		int first = 0;		int last = array.length - 1;		while (first <= last) {			int middle = (first + last) / 2;			if (elementToSearch == array[middle]) {				result = middle;				if (leftOrRightFlag) last = middle - 1;				else first = middle + 1;			} else if (elementToSearch < array[middle]) last = middle - 1;			else first = middle + 1;		}		return result;	}}